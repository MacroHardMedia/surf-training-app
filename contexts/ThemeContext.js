// Re-export everything from the new centralized theme.
// All existing imports of useTheme / ThemeProvider from this file continue to work.
export { ThemeProvider, useTheme, lightTheme, darkTheme } from "../theme/theme";
